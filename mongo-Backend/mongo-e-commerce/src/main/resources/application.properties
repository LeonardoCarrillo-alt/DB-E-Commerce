# MongoDB connection
quarkus.mongodb.connection-string=mongodb://admin:admin123@mongodb:27017/?authSource=admin
quarkus.mongodb.database=ecommerce_multitienda
quarkus.mongodb.min-pool-size=5
quarkus.mongodb.max-pool-size=20

# HTTP
quarkus.http.port=8080

# Jackson
quarkus.jackson.property-naming-strategy=SNAKE_CASE
quarkus.jackson.fail-on-unknown-properties=false

# JWT (validación de tokens desde PostgreSQL)
# mp.jwt.verify.publickey.location=classpath:publicKey.pem
# mp.jwt.verify.issuer=https://bo.com.proj
# smallrye.jwt.sign.key.location=classpath:privateKey.pem

# Docker profile (if needed)
%prod.quarkus.mongodb.connection-string=mongodb://admin:admin123@mongodb:27017/ecommerce_multitienda?authSource=admin

# Logging
quarkus.log.level=INFO
quarkus.log.category."bo.com.proj".level=DEBUG

# Security configuration
quarkus.http.auth.permission.public.paths=/hello,/q/*
quarkus.http.auth.permission.public.policy=permit

# Swagger UI (activado para todos los perfiles
quarkus.swagger-ui.always-include=true
quarkus.swagger-ui.enable=true
quarkus.smallrye-openapi.enable=true

# Perfil desarrollo
%dev.quarkus.mongodb.connection-string=mongodb://localhost:27017/ecommerce_multitienda
%dev.quarkus.http.port=8081
%dev.quarkus.log.level=DEBUG

# PostgreSQL API Client
#postgresql-api/mp-rest/url=http://localhost:8081  # URL del servicio PostgreSQL
#postgresql-api/mp-rest/scope=jakarta.inject.Singleton
#postgresql-api/mp-rest/connectTimeout=5000
#postgresql-api/mp-rest/readTimeout=5000

# Habilitar CORS globalmente
quarkus.http.cors.enabled=true
# Permitir el origen exacto de tu frontend en desarrollo
quarkus.http.cors.origins=http://localhost:5173

# Permitir los métodos HTTP estándar que usas en tus recursos
quarkus.http.cors.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS

# Permitir las cabeceras comunes (incluyendo Authorization para tus interceptores)
quarkus.http.cors.headers=accept,authorization,content-type

# ==============================================
# JWT Configuration
# ==============================================

#mp.jwt.verify.publickey.location=classpath:publicKey.pem
#mp.jwt.verify.issuer=https://bo.com.proj/ecommerce
#mp.jwt.verify.audiences=https://api.bo.com.proj

#smallrye.jwt.sign.key.location=classpath:privateKey.pem
#smallrye.jwt.new-token.lifespan=3600
#smallrye.jwt.new-token.claim-groups=groups

# ==============================================
# PostgreSQL API Client
# ==============================================
#postgresql-api/mp-rest/url=http://localhost:8081
#postgresql-api/mp-rest/scope=jakarta.inject.Singleton
#postgresql-api/mp-rest/connectTimeout=5000
#postgresql-api/mp-rest/readTimeout=5000

# ==============================================
# Security
# ==============================================
#quarkus.security.jwt.enabled=true
#quarkus.security.jwt.token.claims.groups=groups

#quarkus.http.auth.permission.authenticated.paths=/api/*
#quarkus.http.auth.permission.authenticated.policy=authenticated

#quarkus.http.auth.permission.public.paths=/auth/*,/health/*,/swagger-ui/*,/openapi/*
#quarkus.http.auth.permission.public.policy=permit

# CORS
#quarkus.http.cors=true
#quarkus.http.cors.origins=http://localhost:3000
#quarkus.http.cors.methods=GET,POST,PUT,DELETE,OPTIONS
#quarkus.http.cors.headers=accept,authorization,content-type,x-requested-with
